Notes

/corpora is created in ~/nltk_data by 

nltk.download('twitter_samples')
nltk.download('stopwords')

move /corpora to tmp2. 


tmp2 copied from week4.. to one level up. The path is set in the jupyter notebook week1_logistic_regression to ../tmp2
